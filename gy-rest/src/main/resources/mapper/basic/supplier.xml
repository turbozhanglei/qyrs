<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- 参数管理手工映射SQL语句 -->
<!-- 参数管理手工映射SQL语句 -->
<mapper namespace="supplier">


    <sql id="sql_where">
        <if test="null!=supplier_company  and ''!= supplier_company">
            and a.supplier_company like Concat ('%',#{supplier_company},'%')
        </if>
        <if test="null!=supplier_people  and ''!= supplier_people">
            and a.supplier_people like Concat ('%',#{supplier_people},'%')
        </if>
        <if test="null!=carrier_company  and ''!= carrier_company">
            and a.carrier_company like Concat ('%',#{carrier_company},'%')
        </if>
        <if test="null!=supplier_people_mobile  and ''!= supplier_people_mobile">
            and a.supplier_people_mobile like Concat ('%',#{supplier_people_mobile},'%')
        </if>
        <if test="null!=id  and ''!= id">
            and a.id = #{id}
        </if>
    </sql>

    <!-- 插入 -->
    <insert id="saveInfo" parameterType="dto">
        INSERT INTO w_supplier_info (


        <if test="null!=supplier_company  and ''!= supplier_company">supplier_company ,</if>
        <if test="null!=supplier_people  and ''!= supplier_people">supplier_people ,</if>
        <if test="null!=carrier_company  and ''!= carrier_company">carrier_company ,</if>
        <if test="null!=supplier_people_mobile  and ''!= supplier_people_mobile">supplier_people_mobile ,</if>
        create_time
        )
        VALUES
        (


        <if test="null!=supplier_company  and ''!= supplier_company">#{supplier_company} ,</if>
        <if test="null!=supplier_people  and ''!= supplier_people">#{supplier_people} ,</if>
        <if test="null!=carrier_company  and ''!= carrier_company">#{carrier_company} ,</if>
        <if test="null!=supplier_people_mobile  and ''!= supplier_people_mobile">#{supplier_people_mobile} ,</if>
        now()
        )
        <selectKey resultType="java.lang.Long" keyProperty="id">
            SELECT LAST_INSERT_ID() as id
        </selectKey>
    </insert>

    <!-- 删除 -->
    <update id="deleteInfo" parameterType="dto">
        update w_supplier_info a
        set a.is_delete = 'Y'
        where a.id=#{id}
    </update>

    <!-- 修改 -->
    <update id="updateInfo" parameterType="dto">
        update w_supplier_info

        SET
        <if test="null!=supplier_company  and ''!= supplier_company">supplier_company = #{supplier_company} ,</if>
        <if test="null!=supplier_people  and ''!= supplier_people">supplier_people= #{supplier_people} ,</if>
        <if test="null!=carrier_company  and ''!= carrier_company">carrier_company = #{carrier_company} ,</if>
        <if test="null!=supplier_people_mobile  and ''!= supplier_people_mobile">supplier_people_mobile =
            #{supplier_people_mobile} ,
        </if>
        <if test="null!=updator  and ''!= updator">updator = #{updator} ,</if>
        update_time = now()
        where id=#{id}
    </update>

    <!-- 列表查询 -->
    <select id="queryList" parameterType="map" resultType="dto">
        SELECT a.*,a.supplier_company as value,a.id as supplier_id
        FROM
        w_supplier_info a
        WHERE 1 = 1 AND a.is_delete = 'N'
        <include refid="sql_where"/>
    </select>

    <!-- 对象查询 -->
    <select id="getInfo" parameterType="map" resultType="dto">
        SELECT a.*
        FROM w_supplier_info a

        WHERE 1 = 1 AND a.is_delete = 'N'
        <if test="null!=id  and ''!= id">and a.id = #{id}</if>

        limit 0,1
    </select>

    <!-- 列表数量查询 -->
    <select id="queryListCount" parameterType="map" resultType="dto">
        select
        count(1) as total
        FROM w_supplier_info a
        WHERE 1 = 1 AND a.is_delete = 'N'
    </select>

    <!-- 对象查询 -->
    <select id="getInfoBySupplierCompany" parameterType="map" resultType="dto">
        SELECT a.*
        FROM w_supplier_info a
        WHERE 1 = 1 AND a.is_delete = 'N'
        <if test="null!=supplier_company  and ''!= supplier_company">and a.supplier_company = #{supplier_company}</if>
        limit 0,1
    </select>


</mapper>

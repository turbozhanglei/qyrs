<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- 参数管理手工映射SQL语句 -->
<!-- 参数管理手工映射SQL语句 -->
<mapper namespace="wBatchAllot">

    <!-- 插入波次分配记录表 -->
    <insert id="saveInfo" parameterType="dto">
        insert into w_batch_allot (allot_no, batch_id,out_stock_num,sku_id,sale_order_id,create_time,
        creator, is_delete)
        values (#{allot_no}, #{batch_id},
        #{out_stock_num},#{sku_id},#{sale_order_id}, now(),
        #{creator}, 'N')
        <selectKey resultType="java.lang.String" keyProperty="id">
            SELECT LAST_INSERT_ID() as id
        </selectKey>
    </insert>

    <select id="queryList" parameterType="map" resultType="dto">
        SELECT
        a.id,
        a.allot_no,
        f. NAME
        FROM
        w_batch_allot AS a
        LEFT JOIN w_order_sale AS d ON (a.sale_order_id = d.id)
        LEFT JOIN w_order_sale_sku AS z ON (z.sale_id = d.id)
        LEFT JOIN w_order_asn_sku AS f ON (f.order_id = z.order_id)
        WHERE
        d.`status` = 726
    </select>

    <select id="getInfo" parameterType="map" resultType="dto">
        select *,out_stock_num-sub_stock_num as stock_num from w_batch_allot
        where id=#{id}
    </select>

    <update id="updateInfo" parameterType="map">
        update w_batch_allot set sub_stock_num=sub_stock_num+#{sub_stock_num} where id=#{id}
    </update>
</mapper>

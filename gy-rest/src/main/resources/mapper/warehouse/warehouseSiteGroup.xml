<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><!-- 参数管理手工映射SQL语句 -->
<!-- 参数管理手工映射SQL语句 -->
<mapper namespace="warehouseSiteGroup">


    <sql id="sql_where">
        <if test="null!=number  and ''!= number">
            and a.number LIKE concat('%',#{number},'%')
        </if>
        <if test="null!=name  and ''!= name">
            and a.name LIKE concat('%',#{name},'%')
        </if>
        <if test="null!=warehousename  and ''!= warehousename">
            and b.name LIKE concat('%',#{warehousename},'%')
        </if>
        <if test="null!=startDate  and ''!= startDate">
            and DATE(a.create_time) &gt;= #{startDate}
        </if>
        <if test="null!=endDate  and ''!= endDate">
            and DATE(a.create_time) &lt;= #{endDate}
        </if>
        <if test="null!=warehouse_id  and ''!= warehouse_id">
            and a.warehouse_id = #{warehouse_id}
        </if>
        <if test="null!=id  and ''!= id">
            and a.id = #{id}
        </if>
    </sql>

    <!-- 查询 -->
    <select id="queryList" parameterType="map" resultType="dto">
        SELECT a.*,b.`name` as warehousename
        FROM w_warehouse_site_group a
        LEFT JOIN w_warehouse b on a.warehouse_id = b.id
        WHERE 1 = 1 AND a.is_delete = 'N'
        <include refid="sql_where"/>
        ORDER BY id
        <if test="null!=start">
            limit ${start},${end}
        </if>
    </select>

    <!-- 查询 -->
    <select id="getInfo" parameterType="map" resultType="dto">
        SELECT a.*,b.`name` as warehousename
        FROM w_warehouse_site_group a
        LEFT JOIN w_warehouse b on a.warehouse_id = b.id
        WHERE 1 = 1 AND a.is_delete = 'N'
        <include refid="sql_where"/>
        limit 0,1
    </select>

    <!-- 列表数量查询 -->
    <select id="queryListCount" parameterType="map" resultType="dto">
        select
        count(1) as total
        FROM w_warehouse_site_group a
        LEFT JOIN w_warehouse b on a.warehouse_id = b.id
        WHERE 1 = 1 AND a.is_delete = 'N'
        <include refid="sql_where"/>
    </select>

    <!-- 插入 -->
    <insert id="saveInfo" parameterType="dto">
        insert into w_warehouse_site_group (deptid, warehouse_id,
        number, name, nums,
        remark, create_time, creator, is_delete
        )
        values (#{deptid}, #{warehouse_id},
        #{number}, #{name}, #{nums},
        #{remark}, now(), #{creator}, 'N')
    </insert>

    <!-- 修改 -->
    <update id="updateInfo" parameterType="dto">
        update w_warehouse_site_group
        set
        <if test="null!=warehouse_id  and ''!= warehouse_id">
            warehouse_id = #{warehouse_id},
        </if>
        <if test="null!=deptid  and ''!= deptid">
            deptid = #{deptid},
        </if>
        <if test="null!=number  and ''!= number">
            number = #{number},
        </if>
        <if test="null!=name  and ''!= name">
            name = #{name},
        </if>
        <if test="null!=nums  and ''!= nums">
            nums = #{nums},
        </if>
        <if test="null!=remark  and ''!= remark">
            remark = #{remark},
        </if>
        <if test="null!=updator  and ''!= updator">
            updator = #{updator},
        </if>
        update_time = now()
        where id = #{id}
    </update>

    <!-- 删除 -->
    <delete id="deleteInfo" parameterType="map">
        UPDATE w_warehouse_site_group set is_delete = 'Y' WHERE id = #{id}
    </delete>

</mapper>
